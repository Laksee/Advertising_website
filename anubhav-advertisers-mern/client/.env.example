# Only needed in production, or if you don't want to use the Vite dev proxy.
# In local dev, /api/* is proxied to http://localhost:5000 automatically (see vite.config.js).
VITE_API_URL=http://localhost:5000/api
